hosts:
  webserver:
    ssh_address: 127.0.0.1
    ssh_port: 2222
    identifier:
      ssh_user: user
      ssh_password: root
      ssh_key_file: "~/.ssh/id_rsa"
  bastion:
    ssh_address: 127.0.0.1
    ssh_port: 2223
    identifier:
      ssh_user: user
      ssh_password: root
      ssh_key_file: "~/.ssh/id_rsa"
